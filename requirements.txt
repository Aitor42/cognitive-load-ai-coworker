# Dependencies are declared in pyproject.toml ([project.optional-dependencies]),
# which is the single source of truth. This installs LoadGuard with the optional
# API, LLM (Granite), and MCP (IBM Bob) layers.
.[api,llm,bob]
